---
import DefaultPageLayout from '$/layouts/default.astro'
import Prose from '$/components/Prose.astro'

const title = 'About';
const description = 'Unstacked strives to make cloud easy and fun!';
---
<DefaultPageLayout content={{ title, description }}>
    <Prose>
        Unstacked is a one man band run by Fred Stoddart aka Pwed.
    </Prose>
</DefaultPageLayout>

<style>
    .author {
        @apply flex flex-col w-full
    }
</style>
